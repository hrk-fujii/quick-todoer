import React from "react";
import {Center, Text} from "native-base";

const Loading = () => {
    return (
        <Center flex={1}>
            <Text>読み込み中...</Text>
        </Center>
    );
}

export default Loading;
